# Build artifacts produced by deps/build.jl (must NOT be committed,
# otherwise the nested clone becomes a gitlink/submodule that breaks
# `Pkg.add url=...`).
/deps/deps.jl
/deps/usr/
/deps/src/
/deps/build.log

# Julia
/Manifest.toml
